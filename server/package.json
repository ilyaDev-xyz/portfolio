{
  "name": "portfolio-analytics",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "rollup": "node --env-file-if-exists=.env src/cli/rollup.js",
    "test": "node --test --test-reporter=spec 'test/*.test.js'"
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0"
  }
}
